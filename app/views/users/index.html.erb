<h1>Users</h1>
<ul class='users-list'>
  <% @users.each do |user| %>
    <a href='/users/<%= user.id %>'>
      <%=render 'partials/user', user:user %>
    </a>
  <% end %>
</ul>

<%= link_to 'Create post', new_user_post_path(@user) %>